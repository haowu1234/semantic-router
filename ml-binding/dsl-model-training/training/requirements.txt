# DSL Model Training Dependencies (AMD ROCm)
# 
# 安装方式:
#   pip install -r requirements.txt
#
# 验证 GPU:
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.device_count())"

# =============================================================================
# PyTorch ROCm 版本 (AMD GPU)
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/rocm6.2
torch
torchvision
torchaudio

# =============================================================================
# Core ML
# =============================================================================
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
peft>=0.10.0
trl>=0.8.0

# =============================================================================
# Evaluation
# =============================================================================
evaluate>=0.4.0
rouge-score>=0.1.2
sacrebleu>=2.4.0

# =============================================================================
# Utilities
# =============================================================================
pyyaml>=6.0
omegaconf>=2.3.0
wandb>=0.16.0
tensorboard>=2.16.0

# =============================================================================
# Data Processing
# =============================================================================
jsonlines>=4.0.0
pandas>=2.2.0

# =============================================================================
# Progress & Logging
# =============================================================================
tqdm>=4.66.0
rich>=13.7.0

# =============================================================================
# Notes:
# - Flash Attention: ROCm 使用 PyTorch 原生 SDPA (已在 base.yaml 配置)
# - bitsandbytes: ROCm 支持有限，如需量化建议用 GPTQ
# - 如果 ROCm 版本不是 6.2，请修改上面的 URL:
#   - ROCm 6.1: https://download.pytorch.org/whl/rocm6.1
#   - ROCm 6.0: https://download.pytorch.org/whl/rocm6.0
# =============================================================================
